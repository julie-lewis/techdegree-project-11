import React from 'react';

//loading message during loading state
const Loading = () => (
    <div className="not-found">
		<h3>Loading Results</h3>
		<p>Please wait!</p>
    </div>
);

export default Loading;